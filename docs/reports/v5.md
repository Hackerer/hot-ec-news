# v5 报告

日期：2026-03-14

## 本轮自问自答

问题：

- 如果平台偶发失败，这个程序还能不能继续产出日报并保留排障证据？

回答：

- 能。`v5` 增加了重试、失败隔离、错误快照和异常说明区，允许单平台失败但全局任务继续。

## 本轮目标

- 增加采集重试
- 增加失败隔离
- 增加错误快照
- 在日报里展示异常说明
- 完成最终验收文档

## 交付内容

- `withRetries`
- `writeErrorSnapshot`
- 采集器失败不再拖垮整次运行
- 日报异常说明区
- 最终验收文档

## 实际测试结果

已执行：

- `npm run typecheck`
- `npm test`
- 失败注入单元测试：单平台失败时仍生成日报
- `HOT_EC_NEWS_ROOT=/tmp/... npm run run:daily`
- `HOT_EC_NEWS_ROOT=/tmp/... npm run push:report -- --channel wecom --dry-run`
- `HOT_EC_NEWS_ROOT=/tmp/... npm run push:report -- --channel email --dry-run`
- `HOT_EC_NEWS_ROOT=/tmp/... npm run schedule:generate -- --platform macos --time 09:00`

结果：

- TypeScript 类型检查通过
- `vitest` 共 15 个测试全部通过
- 失败注入场景下，采集流程会写入错误快照并保留异常说明
- 干净环境下最终全链路验收通过
