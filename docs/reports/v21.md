# v21 报告

日期：2026-03-14

## 本轮自问自答

问题：

- 既然日报和邮件都改了，为什么还要继续改推送摘要和兼容层？

回答：

- 因为生产里最容易暴露问题的是“拿旧报告重发”。你前面已经实际撞到过一次。新结构必须覆盖摘要推送，而且不能再因为老数据结构缺字段而崩。

## 本轮目标

- 让推送摘要也按类目结构输出
- 补齐旧报告兼容
- 收掉“历史报告一发就炸”的风险

## 交付内容

- `renderPushDigestMarkdown` 切换到：
  - 品类
  - 整体搜索热词 Top15
  - 平台 Top15
- `renderPushDigestMarkdown` / `renderEmailHtml` 增加旧报告兼容
- 推送相关测试更新到新口径

## 实际测试结果

已执行：

- `npm run typecheck`
- `npm test`
- 新增旧报告兼容测试
- 更新 push preview 测试

结果：

- TypeScript 类型检查通过
- `vitest` 共 43 个测试全部通过
- 推送摘要已采用类目优先结构
- 旧报告现在不会再因为缺 `overallItems` / `platformSections` / highlight 字段而崩溃

## 下一步方向

- 做边界场景验收
- 做真实输出验收
- 做 7 轮迭代总收口
